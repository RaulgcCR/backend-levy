<%= form_with(model: score, local: true) do |form| %>
  <% if score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(score.errors.count, "error") %> prohibited this score from being saved:</h2>

      <ul>
      <% score.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :calificado %>
    <%= form.number_field :calificado, id: :score_calificado %>
  </div>

  <div class="field">
    <%= form.label :calificador %>
    <%= form.number_field :calificador, id: :score_calificador %>
  </div>

  <div class="field">
    <%= form.label :calificacion %>
    <%= form.number_field :calificacion, id: :score_calificacion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
